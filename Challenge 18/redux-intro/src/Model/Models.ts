export interface BankAccount{
    balance:number,
    loan:number,
    loanPurpose:string,
    isLoading:boolean
}

export interface Customer{
    fullName:string,
    nationalID:string,
    createdAt:string
}